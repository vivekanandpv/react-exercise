import React, { FunctionComponent } from 'react';

interface NotFoundProps {}

const NotFound: FunctionComponent<NotFoundProps> = (props: NotFoundProps) => {
  return (
    <>
      <h3>NotFound Component</h3>
    </>
  );
};

export default NotFound;
